# Debian Live initrd environment. Keep tracked values empty; use ./secrets.sh --set locally.
# This file is merged at /live.env only when the Debian Live overlay is selected.
# The init-bottom helper copies it privately into /run for the final Live root.

PRESEED_WIFI_PASSPHRASE=''
