# Managed build profile for debian-usb.
PROFILE_NAME="Debian Live"
PROFILE_DISTRO="debian"
PROFILE_VARIANT="live"
PLAN_JSON="live-build.json"
NOTES="Primary bundled Debian live-build profile. Choose Trixie or Forky at runtime; edit the companion JSON when you need to change package lists, mirrors, metadata, or output defaults."
